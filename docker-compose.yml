version: '3.8'

services:
  infraguard-ai:
    build: .
    container_name: infraguard-ai
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped